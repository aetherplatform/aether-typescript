# Aether Identity SDK Preview

Customer-safe OAuth discovery types. Hosted browser authentication remains
standards-based and is not replaced by SDK-owned UI.

Server applications import `ClientCredentialsTokenProvider` and its
configuration types from `@aetherplatform/identity/server`. Client secrets are
never exported from the browser-safe root entry point.
